replicaCount: 1

image:
  repository: ghcr.io/recepgunes1/url-shortener/backend
  pullPolicy: IfNotPresent
  tag: "staging-9e7538a"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

env: []
